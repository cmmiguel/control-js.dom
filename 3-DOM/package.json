{
  "name": "3-DOM",
  "version": "1.0.0",
  "description": "** Sin modificar el body en el archivo index.html **, utiliza javascript para crear un div en el que se muestre un reloj con la hora actual y que se actualice automáticamente cada segundo.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --files 'www' --server 'www'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.7"
  }
}
